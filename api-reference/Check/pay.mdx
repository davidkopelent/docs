---
title: "Pay Check"
---

# Pay Check Endpoint

This page details the POST /checks/{merchantPlaceId}/{deskId} endpoint for processing payments.

<APIRef url="../openapi/checks-api.yaml" method="post" route="/checks/{merchantPlaceId}/{deskId}" />

This renders the interactive API documentation for the "Pay Check" endpoint based on your OpenAPI spec. The endpoint processes payments for checks at the specified merchant place and desk.